---

- name: Adding sudoers file
  copy: src={{ item.src }} dest={{ idem.dest }} owner= {item.owner }} group= {{item.group }} mode={{ item.mode }}
  with_items:
   - { src: 'sudoers', dest: '/etc/sudoers', owner: 'root', group: 'root', mode: '0644' }
